# Copy this template to .env.local when running the extension locally.
# All values are optional - the extension auto-resolves them if not set.

# Google Cloud Project ID (official GCP standard - auto-detected from Firebase config or gcloud)
GOOGLE_CLOUD_PROJECT=

# Region for Cloud Functions and Vertex AI (defaults to FUNCTION_REGION or us-central1)
FIREGEN_REGION=us-central1

# Storage bucket name (auto-detected from Firebase config or gcloud)
FIREGEN_BUCKET=
